export enum BlendMode {
  SourceAtop = 0,
  Multiply = 1,
  Overlay = 2,
  SoftLight = 3,
  HardLight = 4,
  Screen = 5,
}

export enum BlurDirection {
  Vertical = "vertical",
  Horizontal = "horizontal",
  Diagonal = "diagonal",
  Radial = "radial",
  Custom = "custom",
}
